import React from 'react'
import PhotoContainer from './PhotoContainer'

const AllPhotosContainer = (filteredPhotos) => {
    const displayPhotos = filteredPhotos.map( (photo) => {
      <PhotoContainer />
    })

  return (
    {}
  )
}